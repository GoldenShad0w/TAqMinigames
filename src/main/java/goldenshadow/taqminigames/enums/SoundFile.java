package goldenshadow.taqminigames.enums;

/**
 * A record to store data about a sound file
 * @param name The name of the file
 * @param length The length of the sound file in milliseconds
 */
public record SoundFile(String name, int length) {
}
